import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  parentContainer: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100%',
    top: '52px',
    position: 'relative',
  },

});
